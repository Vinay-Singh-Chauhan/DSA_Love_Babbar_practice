#include<bits/stdc++.h>
using namespace std;
#define int long long
#define vi vector<int>
#define vvi vector<vi>
#define pii pair<int,int>
#define umap unordered_map
#define fl(type,symbol,start,end) for(type symbol=start;symbol<end;symbol++)
#define pb(x) push_back(x)
int32_t main()
{
    int test_case;
    cin>>test_case;
    for(int test_c=0;test_c<test_case;test_c++){
        
        }
    return 0;
}

// * following concepts
// *    1> Sieve of Erastothenes
// *    1> Segmented Sieve          [was done already]
// *    1> GDC using Euclid algo    (gcd(a,b)= gcd(a%b,b) where a>b till a!=b)
// *    1> Modular exponentiation   [a^n=(a^(n/2))^2 if n=even else (a^(n/2))^2 *a]
// *    1> Pigeon hole principle [to do]
// *    1> Catalan numbers       [to do]
// *    1> Inclusive and exclusive principle [to do]
